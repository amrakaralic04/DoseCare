{
  "name": "dosecre",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "dev": "live-server public --port=5173 --quiet",
    "format": "echo \"No formatter configured\""
  },

  "dependencies": {
    "firebase": "^12.9.0"
  },

  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
